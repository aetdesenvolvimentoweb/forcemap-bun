/**
 * Constantes relacionadas a autenticação e tokens
 */

/**
 * Tempo de expiração do access token em segundos (15 minutos)
 */
export const ACCESS_TOKEN_EXPIRY_SECONDS = 15 * 60;

/**
 * Tempo de expiração do access token em milissegundos (15 minutos)
 */
export const ACCESS_TOKEN_EXPIRY_MS = 15 * 60 * 1000;

/**
 * Tempo de expiração do refresh token em dias (7 dias)
 */
export const REFRESH_TOKEN_EXPIRY_DAYS = 7;

/**
 * Tempo de expiração da sessão em dias (7 dias)
 */
export const SESSION_EXPIRY_DAYS = 7;

/**
 * Tamanho máximo da informação de dispositivo armazenada
 */
export const MAX_DEVICE_INFO_LENGTH = 100;
