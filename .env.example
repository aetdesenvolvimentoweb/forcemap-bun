# ==========================================
# FORCEMAP API - Environment Variables
# ==========================================

# Node Environment
NODE_ENV=development

# ==========================================
# Server Configuration
# ==========================================

# Porta do servidor (padrão: 3333)
PORT=3333

# Host do servidor (usado apenas para logging)
SERVER_HOST=http://localhost

# ==========================================
# JWT Configuration
# ==========================================

# Secret para assinatura dos access tokens (OBRIGATÓRIO - mínimo 32 caracteres em produção)
# Gere um secret seguro usando: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_ACCESS_SECRET=51dbbbc0c0088714764b17cb42ae0d88f49e3bdd

# Secret para assinatura dos refresh tokens (OBRIGATÓRIO - mínimo 32 caracteres em produção)
# Gere um secret seguro usando: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_REFRESH_SECRET=e07b70481085672d184375a5be41d580a23d88cc

# Tempo de expiração do access token (padrão: 15m - 15 minutos)
JWT_ACCESS_EXPIRY=15m

# Tempo de expiração do refresh token (padrão: 7d - 7 dias)
JWT_REFRESH_EXPIRY=7d

# ==========================================
# Rate Limiting Configuration
# ==========================================

# Máximo de tentativas de login por IP (padrão: 10)
RATE_LIMIT_LOGIN_IP_MAX_ATTEMPTS=10

# Máximo de tentativas de login por usuário/RG (padrão: 5)
RATE_LIMIT_LOGIN_USER_MAX_ATTEMPTS=5

# Janela de tempo para rate limit de login em minutos (padrão: 15)
RATE_LIMIT_LOGIN_WINDOW_MINUTES=15

# ==========================================
# Logging Configuration
# ==========================================

# Nível de log (error, warn, info, debug, trace)
LOG_LEVEL=info

# Pretty print em desenvolvimento (true/false) - requer pino-pretty instalado
LOG_PRETTY=true

# ==========================================
# CORS Configuration
# ==========================================

# Development CORS Settings
# Domínios permitidos em desenvolvimento (separados por vírgula)
CORS_ALLOWED_ORIGINS_DEV=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://localhost:8080

# Permite cookies/credenciais em desenvolvimento (true/false)
CORS_ALLOW_CREDENTIALS_DEV=true

# Tempo de cache para preflight em desenvolvimento (segundos)
CORS_MAX_AGE_DEV=86400

# Production CORS Settings
# Domínios permitidos em produção (separados por vírgula) - OBRIGATÓRIO CONFIGURAR
CORS_ALLOWED_ORIGINS_PROD=https://yourdomain.com,https://www.yourdomain.com,https://app.yourdomain.com

# Permite requisições sem origin em produção - para mobile apps/Postman (true/false)
CORS_ALLOW_NO_ORIGIN_PROD=false

# Permite cookies/credenciais em produção (true/false)
CORS_ALLOW_CREDENTIALS_PROD=false

# Métodos HTTP permitidos em produção (separados por vírgula)
CORS_ALLOWED_METHODS_PROD=GET,POST,PUT,DELETE,PATCH

# Headers permitidos em produção (separados por vírgula)
CORS_ALLOWED_HEADERS_PROD=Content-Type,Authorization,X-Requested-With,Accept

# Headers expostos em produção (separados por vírgula)
CORS_EXPOSED_HEADERS_PROD=

# Tempo de cache para preflight em produção (segundos)
CORS_MAX_AGE_PROD=86400

# ==========================================
# Example Production Values
# ==========================================
# CORS_ALLOWED_ORIGINS_PROD=https://meuapp.com,https://www.meuapp.com,https://admin.meuapp.com
# CORS_ALLOW_NO_ORIGIN_PROD=true
# CORS_ALLOW_CREDENTIALS_PROD=true
# CORS_EXPOSED_HEADERS_PROD=X-Total-Count,X-Rate-Limit-Remaining