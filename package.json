{
  "name": "forcemap",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.12",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "wrangler": "^4.4.0"
  }
}